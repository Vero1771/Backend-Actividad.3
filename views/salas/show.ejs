<!DOCTYPE html>
<html>
  <head>
    <title>Sala <%= sala.name %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Sala: <%= sala.name %></h1>
    <a href="/admin">Volver</a>
    <p>ID: <%= sala.id %></p>
    <p>Capacidad: <%= sala.capacity %></p>
    <p>Precio por asiento: <%= sala.price.toFixed(2) %></p>
  </body>
</html>
