<!DOCTYPE html>
<html>
  <head>
    <title>Registrarse</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Registrarse</h1>
    <form id="registerForm">
      <label>Email: <input type="email" name="email" required></label><br>
      <label>Password: <input type="password" name="password" required></label><br>
      <button type="submit">Registrar</button>
    </form>
    <script>
      document.getElementById('registerForm').addEventListener('submit', async e => {
        e.preventDefault();
        const form = e.target;
        const data = { email: form.email.value, password: form.password.value };
        const res = await fetch('/auth/api/register', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) });
        const j = await res.json();
        if (res.ok) {
          localStorage.setItem('token', j.token);
          // set cookie so server-side views can read token
          document.cookie = 'token=' + j.token + '; path=/';
          // redirect admin users to admin dashboard
          if (j.user && j.user.role === 'admin') {
            window.location = '/admin';
          } else {
            window.location = '/';
          }
        } else {
          alert(j.error || j.warning || 'Error');
        }
      });
    </script>
  </body>
</html>